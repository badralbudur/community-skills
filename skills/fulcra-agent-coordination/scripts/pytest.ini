[pytest]
# Run from this directory: `python3 -m pytest`
# `coord_engine` is imported from here; `tests/` is on the path for the shared
# helper module (a plain module, not a plugin, so it needs sys.path not conftest).
addopts = --import-mode=importlib
testpaths = tests
pythonpath = . tests
