#!/usr/bin/env python3
"""Entry point for the vendored coord-engine."""
import os
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

from coord_engine.cli import main

if __name__ == "__main__":
    sys.exit(main())
